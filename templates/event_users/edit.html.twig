{% extends 'base.html.twig' %}

{% block title %}Edit Events{% endblock %}

{% block body %}
    <h1>Edit Events</h1>

    {{ include('event_users/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_event_users_index') }}">back to list</a>

    {{ include('event_users/_delete_form.html.twig') }}
{% endblock %}
